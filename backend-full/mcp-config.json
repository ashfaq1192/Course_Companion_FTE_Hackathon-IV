{
  "mcpServers": {
    "course-companion": {
      "command": "python",
      "args": ["-m", "src.mcp_server"],
      "cwd": ".",
      "env": {
        "DATABASE_URL": "${DATABASE_URL}"
      }
    }
  }
}
